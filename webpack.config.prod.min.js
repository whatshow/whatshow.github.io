//导入第三方函数库
const webpack = require('webpack');
//导入配置
let webpackConfig = require("./webpack.config");
//生成文件增加hash值避免重复 & 增加min
webpackConfig.output.filename =  'bundle.[hash:5].min.js';
//导出新配置
module.exports = webpackConfig;